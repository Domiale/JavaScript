/* 
*Operadores Logicos
  Los operadores logivos permiten evaluar expresiones que dan como resultado "verdadero" o "falso". Una expresion que es un trazo de un codigo que evalua, es deci resulta en un "true" o "false"

1. AND (&&) - Devuelve "true" solo si ambos operandos son verdaderos
2. OR (||) - Devuelve "true" si al menos uno de los operandos es verdadero
3. NOT (!) - Invierte el valor booleando. Se escribe del lado izquierdo de la variable sin espacios.
*/

let a = 5 < 1; //false
let b = 5 > 7; //false
/* console.log((a) && (b)); */ //true

/* console.log(a || b); */ //false

console.log(!b); //false